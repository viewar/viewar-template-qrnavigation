import styled, { css } from 'styled-components';

export const Container = styled.div`

  pointer-events: all;
  width: 100vw;
  height: 100vh;
  color: green;
  position: relative;
  overflow: hidden;
 
`;
